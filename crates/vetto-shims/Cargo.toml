[package]
name = "vetto-shims"
version = "0.2.16"
edition = "2021"

[dependencies]
vetto-core = { path = "../vetto-core" }
thiserror = "1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
libc = "0.2"
